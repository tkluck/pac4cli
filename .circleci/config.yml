version: 2
jobs:
  test:
    docker:
      - image: rust
    steps:
      - checkout
      - run: apt update
      - run: apt install --yes libpacparser-dev virtualenv libsystemd-dev libdbus-glib-1-dev libgirepository1.0-dev libdbus-1-dev dbus netcat-openbsd python3-dev
      - run: make check
workflows:
  version: 2
  build_and_test:
    jobs:
      - test
